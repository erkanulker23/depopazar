# =============================================================================
# DepoPazar – Ortam değişkenleri referansı
# Her domain/subdomain = ayrı proje = ayrı .env (backend/.env veya deploy dizini)
# Asıl backend .env şablonu: backend/.env.example
# =============================================================================

# Uygulama (her kurulum için farklı)
APP_NAME=DepoPazar
APP_ENV=production
APP_DOMAIN=firma1.depopazar.com
APP_URL=https://firma1.depopazar.com

# Veritabanı – her kurulum ayrı DB (paylaşım yok)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=depopazar_firma1
DB_USERNAME=
DB_PASSWORD=

# JWT
JWT_SECRET=
JWT_REFRESH_SECRET=
JWT_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=7d

# Uygulama
NODE_ENV=development
PORT=4100
FRONTEND_URL=http://depotakip-v1.test

# CORS (production’da zorunlu): FRONTEND_URL veya CORS_ORIGINS
# CORS_ORIGINS=https://firma1.depopazar.com

# Swagger – production’da false
SWAGGER_ENABLED=true
