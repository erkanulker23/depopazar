# =============================================================================
# DepoPazar – Tek .env (proje kökü)
# Yeni kurulum: Bu dosyayı .env yapıp değerleri doldurun. Bkz. docs/ENV-TEK-DOSYA.md
# =============================================================================

# Uygulama (her kurulum için farklı)
APP_NAME=DepoPazar
APP_ENV=production
APP_DOMAIN=general.awapanel.com
APP_URL=https://general.awapanel.com

# Veritabanı – Forge’da oluşturduğunuz DB bilgileri
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=general
DB_USERNAME=
DB_PASSWORD=

# JWT – openssl rand -base64 48 ile üretin
JWT_SECRET=
JWT_REFRESH_SECRET=
JWT_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=7d

# Uygulama – canlıda NODE_ENV=production zorunlu
NODE_ENV=production
PORT=4100
FRONTEND_URL=https://general.awapanel.com

# Swagger – canlıda false
SWAGGER_ENABLED=false

# Frontend (build anında okunur; tek .env proje kökünde)
# Aynı domain'de: VITE_API_URL boş bırakılabilir (origin + /api kullanılır).
# Farklı API domain: VITE_API_URL=https://api.site.com/api
VITE_API_URL=
