# Laravel Forge Nginx – Sadece root satırı (kopyala-yapıştır)
# Bu proje Laravel DEĞİL; web kökü "public" değil "php-app/public".
# Nginx config'inizde root satırını aşağıdaki gibi yapın:

root /home/forge/celebi.awapanel.com/php-app/public;

# YANLIŞ (403 verir):
# root /home/forge/celebi.awapanel.com/public;
